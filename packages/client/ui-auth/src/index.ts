/**
 * Gateway-auth account chip, node half. Empty apply is the Loader seat; the
 * browser half occupies `sidebar.footer.action` through `exports["./client"]`.
 */

/** Host plugin body — browser presentation only. */
export function apply(): void {}
