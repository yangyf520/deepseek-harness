/**
 * Package entry — no host-plane body; the plugin ships only a client bundle.
 * @module @deepseek-ai/dsh-client-ui-audit-review
 */

/** Host plugin body — no host-side behavior for this surface plugin. */
export function apply(): void {}
